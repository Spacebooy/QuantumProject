# Copy to .env and set a random secret, e.g. python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=
# Local development defaults to SQLite; Docker uses PostgreSQL.
DATABASE_URL=sqlite:///./quantum.db
POSTGRES_DB=quantum_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5434
